name: Pricing compatibility paused for kernel reset

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Paused until K8
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pricing compatibility resumes with the K8 kernel owner."
