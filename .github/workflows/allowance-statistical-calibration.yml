name: Allowance calibration paused for kernel reset

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    name: Paused until K8
    runs-on: ubuntu-latest
    steps:
      - run: echo "Allowance calibration resumes with the K8 kernel owner."
