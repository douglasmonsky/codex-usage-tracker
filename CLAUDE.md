# Claude Code Instructions

Read and follow [`AGENTS.md`](AGENTS.md) before changing this repository.

The task-specific GitNexus workflows under `.claude/skills/gitnexus/` are
repository-local reference material. Load only the skill matching the current
task. Refresh the graph with `gitnexus analyze --index-only .` so routine
indexing does not rewrite repository guidance.
